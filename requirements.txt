# Install the contextlens package from local source
.
# Additional runtime deps (already in pyproject.toml but listed explicitly
# so HF Spaces installs them even before running pip install .)
gradio==5.9.1
pandas>=2.0.0
tiktoken>=0.7.0
anthropic>=0.30.0
openai>=1.35.0
rich>=13.7.0
click>=8.1.0
jinja2>=3.1.0
